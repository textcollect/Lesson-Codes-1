package BasicOOP;

public class BasicObject {
	public static void main(String[] args) {
		/*
		eCommerce website objects:
		Product, Customer, Category, Payment, Shopping Cart

		Product (states):
		- id, name, price, description, type, image url, quantity, etc.
		- if we have 100 products, all will have the same properties

		Product (methods):
		- checkQuantity()
		- addProduct() -> execute checkQuantity(), if return 20(assumed limit), then do not add product
		- editProduct()
		- deleteProduct()
		- displayProduct() -> execute checkQuantity(), if return 0, then do not show the product
		- setDiscountPrice()

		 */
	}
}
